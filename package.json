{
  "name": "gadgethub-monorepo",
  "version": "1.0.0",
  "description": "GadgetHub Monorepo root package.json for automated deployment",
  "scripts": {
    "postinstall": "npm install --prefix backend",
    "build": "npm install --prefix backend",
    "start": "npm start --prefix backend"
  }
}
